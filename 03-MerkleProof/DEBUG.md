## 🐛 `DEBUG.md` – 디버깅 & 학습 회고

```markdown
# 🐞 DEBUG – MerkleProof.circom

## ✅ 성공 흐름 요약

- Poseidon 해시 사용 방식 학습 (입력 개수 명시 필요)
- 반복문 내 조건 분기 없이 signal 기반 논리 구성
- 중간 `left[i]`, `right[i]` 도 `signal`로 선언해야 제약 조건에 포함됨

## 🧠 학습한 핵심 개념

- `var`는 회로 제약 조건을 생성하지 않음 → 디버깅용 계산에만 사용됨
- Merkle Tree는 포함 증명만 가능, 실행 유효성 검증은 별도 ZKP 필요
- 라이트 노드는 루트 해시만으로 트랜잭션 검증이 가능함

## 🪛 주요 시행착오

- ❌ `left`, `right`를 `var`로 선언 → 회로 제약 조건 미생성 → 검증 실패
- ✅ `signal` 배열로 선언 후 포지션 조건식 적용 → 성공적으로 증명

## 🔄 적용 가능성

- ZK-DID 또는 ZKBridge에서 상태 루트 증명 회로 설계 시 핵심 기초로 활용 가능
```
